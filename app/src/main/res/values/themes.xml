<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/trace_2_</item>
        <item name="postSplashScreenTheme">@style/Theme.FitSync</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>
        <!-- Duration of the splash screen animation -->
        <item name="windowSplashScreenAnimationDuration">1000</item>
    </style>

    <style name="Theme.FitSync" parent="android:Theme.Material.Light.NoActionBar" />
</resources>